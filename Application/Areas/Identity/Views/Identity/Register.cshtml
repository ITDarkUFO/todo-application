@model Application.Models.User

@{
    ViewData["Title"] = "Регистрация";
}

<div class="d-flex justify-content-center mt-5 mb-5">
    <h1 class="d-inline-block">Зарегистрироваться</h1>
</div>

<form asp-action="Register" method="post">
    <div class="row mb-2">
        <div class="col-12 col-xl-6 m-auto">
            <div class="form-floating">
                <input asp-for="UserName" class="form-control" placeholder="Имя пользователя" />
                <label asp-for="UserName">Имя пользователя</label>
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
        </div>
    </div>
    <div class="row mb-2">
        <div class="col-12 col-xl-6 m-auto">
            <div class="form-floating">
                <input asp-for="Email" class="form-control" placeholder="Почта" />
                <label asp-for="Email">Почта</label>
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
        </div>
    </div>
    <div class="row mb-2">
        <div class="col-12 col-xl-6 m-auto">
            <div class="form-floating">
                <input name="password" required class="form-control" placeholder="Пароль" type="password" />
                <label for="password">Пароль</label>
            </div>
        </div>
    </div>
    <div class="row mb-2">
        <div class="col-12 col-xl-6 m-auto">
            <div class="float-end">
                <input type="submit" value="Зарегистрироваться" class="btn btn-primary px-5" />
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12 col-xl-6 m-auto">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        </div>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
